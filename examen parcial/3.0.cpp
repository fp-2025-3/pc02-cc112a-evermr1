#include <iostream>
using namespace std;

bool FD (int(*M)[N],int f);
for(int i=0;i<N;i++){
    for(int j=0;j<N;j++){
        for(int a=0;a<n;a++){
    while ((M[f][j]) > M[i][a]){
        if (((M[f][j]) >= M[i][j])  &&(i != f)){
            cout << "es dominante" << endl;
            
        }
    }
        }
    }
}

                              int
                              main()
            {
                int N;
                int M[N][N] = {(2, 3, 1, 4),
                               (1, 2, 3, 2),
                               (5, 6, 7, 8),
                               (0, 1, 2, 1) }


                return 0;
            }
